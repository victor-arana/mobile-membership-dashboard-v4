export interface TransactionDistribution {
    parentLevel: number;
    level: number;
    elementId: number;
    elementName: string;
    regularTransactions: number;
    mobileTransactions: number;
}
